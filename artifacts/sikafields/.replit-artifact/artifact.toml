kind = "web"
previewPath = "/"
title = "SikaFields — Climate-Tech Carbon Platform"
version = "1.0.0"
id = "artifacts/sikafields"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 18573
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/sikafields run dev"

[services.env]
PORT = "18573"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/sikafields", "run", "build"]
publicDir = "artifacts/sikafields/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
